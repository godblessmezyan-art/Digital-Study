# 数字书房 · 项目进度（progress.md）

> 迭代历史、Phase 状态与遗留技术债。历史完整记录见 `docs/handoff.md`。

## 1. 当前里程碑

| 里程碑 | 内容 | 状态 |
|---|---|---|
| Phase 0 | git 基线 + 蓝图入库 + 分支 | ✅ 完成 |
| Phase 1 | 环境层重构（`.room` + `--room-*` 全量变量化） | ✅ 完成（提交 `a65c869`，当前线上版本） |
| Phase 1.5 | 环境层 v2（整张书房场景图统一背景） | ✅ 完成（备份于 tag `v2-final-archive` → `9576065`） |
| 版本/部署 | main 分支发布 + `deploy.ps1` 一键发布 | ✅ 完成（提交 `af69c3f`） |
| 结构重构 | 迁移为 my-website/ monorepo（frontend Vite + backend Express 骨架） | 🔄 进行中（分支 `refactor/my-website`） |
| 设计图 02 | 温暖手绘风重构：删除 .room 环境层（棕色地板穿透书卡的根源）；容器 min(94vw,1500px)；侧栏 232→120px；暖橙色板（#D88A3D/#5A4632/#F8F0DF）；Hero 左文右图（hero-warm.png）；cover.js 水彩书封接线；书架改平铺 4 列完整卡片 | ✅ 完成（本地，截图 .shots/w02-*.png，待验收发布） |
| Phase 2A | 响应式骨架 + 主题架构分离：themes/warm-study.css（裸色值/env/asset 令牌唯一来源）+ variables.css 纯语义层；容器 min(94vw,1880px)；Nav 208px 家具化（书签条选中态）；断点 1100/900/600/400；移动端 Nav 横条/Hero 纵排/Stats 2×2/分类横滑/书架 2 列；7 档视口 puppeteer 实测零横向溢出（.shots/p2b-*.png）；hero.png AI 水印已修补 | ✅ 完成（本地，待用户验收） |
| Phase 3 | 氛围感升级：① Nav 208→264px「书房侧厅」（我的分类真实数据联动书架 + 底部名言 + 盆栽，书单→书摘）；② 插画拆出 Hero 卡片壳，改为 body 级 .env-art 环境层通铺右上区域（mask 羽化融入纸面，绝对定位随文档滚动不横穿内容，≤900px 隐藏）；③ Hero 去 Banner 化（文字直接落在纸面）；④ 右上角今日书签手记卡（胶带+仿旧纸，数据取 bookmarks[0] 不新增）；移动端手记卡随流嵌入 Hero 卡片 | ✅ 完成（7 档视口零溢出，截图 p2b-*.png） |
| 六主题系统 | ① 六套主题令牌 CSS（themes/fresh-natural、warm-study、night-library、minimal-white、retro-paper、dream-illustration）+ 每主题专属插画（全部 AI 水印已检测修补）；② dream-illustration 为全屏背景模式（.env-art 转 fixed 铺满视口，含移动端，卡片加 backdrop-blur）；③ 设置面板 settings.js（顶栏 ⚙ / 侧栏「设置」打开，6 张主题卡即时切换，setTheme 广播 study:theme 事件，Hero 图随主题换）；④ 表面细节全令牌化（search-bg/tape/progress-track/avatar 等），无裸色值残留 | ✅ 完成（六主题 1920 + dream 390 实测零溢出，截图 th-*.png） |
| 顶栏去卡片化 | 用户反馈「顶部和左侧两个明显卡片」：顶栏去圆角/阴影/底板，改为纸面一行（吸顶时 color-mix 主题色轻纱+毛玻璃保可读）；「书房」品牌只保留顶栏左上角一处，侧栏铭牌删除，菜单直接从「首页」开始 | ✅ 完成（1920/390 截图复验） |
| 全宽环境层 | 用户反馈「背景只在右侧死板无空间感，应延伸到顶栏和侧栏后方」：每主题新生成 2560×1080 四角环绕全景图（左上垂叶/左下盆栽/右侧主场景/右下蕨叶，中心留白，全部水印已修补），新增 --asset-env 令牌；.env-art 改全宽 min(88vh,920px)，仅底缘羽化，上/左/右贴边；顶栏与侧栏半透明面板浮在房间上（dream 主题仍为 fixed 全屏模式） | ✅ 完成（六主题 1920 零溢出，左上角裁剪确认背景延伸到 Nav/侧栏后方，截图 th-*.png） |
| Phase 2 | Hero 场景重做（SVG 替换 hero.png） | 待办 |
| Phase 3 | 中景统一与区块缝合 / Nav 融入 | 待办 |
| Phase 4 | 前景归位与减法（只删不加） | 待办 |
| 后端开发 | Express 骨架 → 书架/阅读数据 API → 数据库接入 | 待办 |

## 2. 迭代历史（为什么现在长这样）

| 轮次 | 做了什么 | 用户反馈要点 |
|---|---|---|
| 1 | 首版搭建：9 大区块、组件化、主题变量预留 | 方向对，但… |
| 2 | 用户判定"太像 SaaS 后台"，13 条纠偏 | 必须至少 2 轮截图迭代 |
| 3 | 两轮迭代：选中态/胶囊按钮/厚卡片修正 | "明显接近目标" |
| 4 | 6 项精修：侧栏 232px 锚点、内容区 1048px、Hero 去卡片感、统计改「我的阅读生活」 | 只做比例与层级，不加东西 |
| 5 | **信息层级反转**（书籍优先）：新增「继续阅读」；侧栏改私人书房目录 | 用户给出目标结构图 |
| 6 | 空间感深化：房间氛围层、主书封实体感、书架架板、便签、手记 | "还像高级 Dashboard，不像书房" |
| 7 | 关系重构：Hero 左右出血 + 前景盆栽/书堆、继续阅读叠压 | 要求元素可跨越区域、有纵深 |
| 8-10 | **环境底层重做**：三件套 → `.room` 连续房间；卡片半透明纸感 | "不要通过增加元素解决空白，要重新设计整个背景空间" |
| 11 | 结构级重构：Hero 改"房间开口"、侧栏墙面化、24 本书改书墙、清理孤儿 CSS | "不要在现有页面上不断打补丁；如果布局结构阻碍设计，直接重构" |

## 3. 版本与发布约定

- **main** 指向线上版本（当前 `a65c869` 环境层 v1）；`refactor/my-website` 为本次结构重构分支，验收后并入 main。
- **备份**：tag `v2-final-archive`（`9576065`）保留"最终版"（环境层 v2 界面），已推远程。
- **发布**：`.\deploy.ps1` 一体化（git 提交推送 → 前端构建 → scp 上传 `dist/` 到 `wxhappylife.top:/opt/1panel/apps/openresty/openresty/root/test/` → HTTP 200 验证）。

## 4. 遗留技术债（建议顺序处理）

1. **`hero-warm.png` 约 2.5MB 未压缩**：上线前压缩或转 WebP（已复制到 themes/warm-study/hero.png 并修补水印，旧 hero.png / hero-warm.png 可删）。
2. **`.room` 环境层已删除**（设计图 02 轮）：`--room-*` 变量组一并移除，handoff.md 中相关描述已过时；若未来想恢复“连续房间”需重新设计。
3. **孤儿 CSS**：`.note-slip`、`.recent-row*`、`.desk-note`（便签已随设计图 02 移除）、`.shelf-row__board`（架板已移除）等样式残留待清理。
4. **纯装饰性交互未实现**（原型阶段刻意为之，别当 bug 修）：搜索框无过滤、侧栏「书单/时间线/设置」无 target、Hero 按钮无 click。
5. **侧栏选中态**：主目录项共用 `.is-active`，加路由/多页时需重构。
6. **暗色模式无入口**：theme.js 机制保留（night 主题变量已预置暖棕色版），但顶栏未放切换按钮。
7. **部署脚本与 Vite 联动**：deploy.ps1 已改为构建后上传 `frontend/dist/`，若前端资源结构再变需同步维护。

## 5. 下一轮开工自检清单

- [ ] 是否加了功能/组件/动画/颜色？（除非用户要求，应为"否"）
- [ ] 改动是否只落在语义变量与既有文件结构内？
- [ ] 去掉所有文字和按钮，页面还像一间书房吗？
- [ ] 第一眼看到的是"书"，还是"数据/筛选器"？
- [ ] 是否又造出一个孤立白卡片？
- [ ] `cd frontend; npm run dev` 起服务无 JS 报错（Console 干净）？
- [ ] 是否截了图、真的看过、并至少迭代一轮？
- [ ] 是否向用户说明：改了什么 / 为什么 / 哪里仍需优化？

---

## 2026-09-03 · 01 主题书籍区域「私人书房化」（用户验证制）

**范围**：只动 fresh-natural 主题下「继续阅读 + 我的书架」的视觉表现，全部样式写在 css/themes/fresh-natural.css 内以 [data-theme="fresh-natural"] 作用域隔离；基础 components.css / DOM / 数据 / 交互零改动，02~06 主题不受影响。

**书卡去 Dashboard 化**：
- .book-card 卡壳消失（无底色/无阴影/overflow visible），书封成为唯一主体
- 封面：书脊渐变（左缘合页线）+ 纸纹叠加 + 3px/6px 不对称圆角 + 纸页堆叠影（2px 米色硬边 + 轻软影）
- 错落：6n 循环 ±0.3~0.8° 微倾 + 高度 ±1~2% 微差，顶缘对齐成安静主线（align-items:start，避免长书名行错位）
- 信息层级：书名衬线 16px（长名两行 line-clamp）> 作者 12px > 章节 meta 弱化 > 进度 2px 细绿线 + 小字百分比；已读完进度线转暖金
- hover：整卡只抬 2px、封面转正 + 落地影加深，无放大无发光

**继续阅读区**：continue-wrap 变成一张淡米黄纸（纸纹+暖白渐变+发丝边框），主书卡坐在纸上（小圆角+轻浮起），右侧最近翻开列表融入纸底、发丝虚线分隔；主书封 -1.2° 倾斜 + 纸页堆叠影。

**书架区**：底部加极淡暖色径向底晕（架板受光暗示），不做实线架板，克制。

**验证**：按新协作规则由用户亲自验证（1440/1920、书少/书多、长书名、hover、主题切换回归）。

---

## 2026-09-03 · 十条反馈系统对齐设计图 02（背景退后 / 内容收紧 / 书成主体）

用户十条反馈的核心：背景太抢、Hero 空、Sidebar 重、Header 散、分类占太高、继续阅读左大右小失衡、书封跑偏成几何色块、色偏灰绿、透明度过高导致糊、缺书房秩序感。

**环境层降噪（layout.css）**：.env-art opacity 1 → 0.42，mask 改上下双缘羽化——背景退为氛围，UI 成为主体。dream-illustration 有主题内 `opacity:1; mask:none` 覆盖，全屏梦境不受影响。

**透明度治理**：--color-nav-bg / --color-side-bg 提到 0.97/0.96（warm-study + fresh-natural），侧栏去掉 color-mix 透明+blur，恢复不透明纸面+轻影；搜索框从"全透明"改回轻纸面胶囊（发丝边框+88% 实心底），边界清晰。

**垂直节奏压缩**：.page gap 40→30，.section gap 18→14，Hero min-height 430→330、文字区 padding 56/48→30/26，统计卡 padding 18/20→14/18。

**Header 收敛（top-nav.js）**：删铃铛按钮、删 Ctrl+K kbd 提示（快捷键功能保留），只剩 品牌 + 搜索 + 设置/头像，一行安静工具栏。

**分类压缩成筛选条（categories.js + components.css）**：标题+计数同一行（section-head），.section--compact 标题 18px；芯片 padding 8/18→5/14、图标圆底取消、去阴影。

**继续阅读 = 四本平等的书架排列（continue-reading.js 重写模板）**：废弃 continue-wrap 大卡+窄列表结构，改为 .continue-grid 四列，卡片与 .book-card 完全同构——两个书区共用一套视觉语言。components.css 删除全部 continue-featured/list/item 旧样式（-148 行），断点 ≤900 转 2 列；fresh-natural / dream 主题内旧选择器同步清理。

**书封换温暖手绘插画**：cover.js 从内联几何 SVG 改为 `<img>` 引用 assets/covers/1-6.png（湖畔晨光/金色沙丘/山村黄昏/茶馆一隅/林间空地/星夜小屋，1024x1536 手绘水彩，暖米黄+赭石+鼠尾草绿低饱和）；onerror 自移除露出 --cover-N 渐变兜底。6 张全部检出「Qoder AI 生成」水印（1/2/4/5/6 右下 + 6 顶部 + 3 右下白色标签底板），用局部离群检测+克隆羽化修补；3.png 首扫被屋顶纹理干扰，改窄窗+白底检测两步修补，与原图对比确认屋顶连续无误伤。fresh-natural 书脊/纸纹改 ::after 叠加（PNG 不透明后 background-image 会被盖住）。

**未动**：路由/数据结构/搜索/分类联动/收藏/主题切换逻辑；02~06 主题除 dream 清理死选择器外零改动。

**验证**：由用户亲自执行（1440/1920、书少/书多、长书名、hover、六主题切换回归）。

---

## 2026-09-03 · 01 主题第二轮精修：构图/比例/层级严格对齐设计图

用户十二节任务书核心：Hero 过高、背景仍抢、手记突兀、统计像 SaaS、分类像 Filter Bar、继续阅读要强化书架感、宽屏比例、Sidebar/Header 减重；只改视觉层，01 作用域优先，02~06 不破坏。

**§7 宽屏比例（全局）**：--page-max 1880 → 1560。Hero/统计/分类/书籍共用同一内容边界，1920/2560 下不再拉成长横幅，也不留大片侧空白。

**§1 Hero 压缩（全局）**：min-height 330→250，文字区 padding 18/14，标题 clamp 上限 38→34px，desc/actions 间距收紧。1080 高首屏可自然看到「继续阅读」。

**§2 背景层级（01 作用域）**：fresh-natural 下 .env-art opacity 0.26 + blur(1.5px) contrast(0.9) saturate(0.85)——像淡淡的室内光影；未用白色蒙层（用户明令禁止雾蒙蒙）。全局 0.42 保持，dream 全屏覆盖不受影响。

**§3 手记便签化（01）**：300→252px、纸纹底、边框/阴影减半、tag 去胶囊壳、字号全线缩小——书房小物件而非 Widget。

**§4 统计轻量信息栏（01）**：stat-card 去底色/去阴影/去圆角，底部发丝线收住；图标 42→32、数字 25px 为主角、label 弱化；hover 只变线色不上浮。

**§5 分类目录索引（01）**：芯片无边框无阴影，静止态淡绿纱底融进纸面；选中态 primary-soft 淡绿底+深绿字（不再是实心深绿块）；高度再降。

**§6 继续阅读书架感（共享）**：抽出 components/book-card.js 共享模板（书架+继续阅读同构），新增收藏小心形 ♡（点击切换 ♥ 点亮，内存态轻量交互，不接数据层）；基础样式在 components.css，01 精修（柔和蔷薇红点亮色）在主题内。进度细线绿色。

**§8 Sidebar 减重（01）**：side-nav 改 78% 纸底 + blur(10px) + 极轻影，融入环境；导航文字保持清晰，选中态淡绿不变。

**§9 Header 融合（01）**：搜索框 380→280px、底色 72% 更轻。

**未动**：路由/数据结构/API/搜索/分类联动/阅读/笔记/主题切换；02~06 主题样式零改动（心形有中性基础样式兜底）。

**验证**：由用户亲自执行（验收清单 ①~⑩）。

---

## 2026-09-03 · 首页信息架构重定位：阅读入口 + 独立书架页（hash 路由）

用户判定首页存在 Dashboard 式纵向堆叠：首页不应承担完整书架管理职责。重定位为「阅读入口 / Continue Reading」，视觉优先级 Hero → 继续阅读 → 阅读状态 → 今日手记。

**路由（main.js）**：静态原型最小 hash 路由，双视图互斥——`#/` 首页（Hero/继续阅读/统计/今日书签四段），`#/shelf` 书架页（搜索工具条+分类筛选条+全部 24 本）。hashchange 切视图+侧栏选中态同步+回顶。首页不再渲染分类区与 24 本书网格。

**书架页（新 components/shelf-page.js）**：组合 renderCategories()（分类条整体迁来）+ renderBookGrid(books)；页内搜索框（320px 纸面胶囊）按书名/作者过滤；分类事件（页内芯片+侧栏「我的分类」）与搜索事件（study:search）叠加过滤。

**bookshelf.js 重构为受控网格**：renderBookGrid(list) 暴露 el.paint(list)，过滤逻辑上收到 shelf-page；首页/书架页可复用。

**侧栏（side-nav.js）**：首页/书架/分类三项改视图切换（data-view），笔记/书摘仍为首页锚点，设置开主题面板；新增 syncNav(view) 供路由同步选中态；「我的分类」点击先切书架视图再派发 study:category（事件照旧，唯一组件间事件契约未破坏）。

**顶栏搜索接线**：input 时派发 study:search；在首页输入自动跳转书架页。Ctrl+K 保留。

**Hero 按钮接线**（原为静态）：「开始阅读」→ 平滑滚到 #sec-continue；次按钮改「去书架逛逛」→ #/shelf。

**Main 阅读宽度**：.page max-width 1080 居中（外层骨架 min(94vw,1560px) 不变）——宽屏下主内容保持稳定阅读宽度，两侧由环境插画补氛围；.page > .view 承接原 section 流式布局，≤1100 断点同步。

**01 主题**：架板底晕从 #sec-shelf 迁到 .shelf-page；封面微倾错落规则扩展到 .continue-grid（4n 循环）——首页继续阅读也有手作摆放感。

**未动**：数据结构/API/主题切换/收藏心形逻辑；02~06 主题样式零改动（路由与视图为全局结构，各主题下同样生效）。

**验证**：由用户亲自执行——重点：首屏多快看到「我正在读什么」、书架页筛选+搜索组合、侧栏三视图项切换、顶栏搜索跨视图跳转。

---

## 2026-09-03 · 01 主题第三轮渐进精修：实体书 / 设计感错落 / 局部雾化 / 纸质目录

用户要求渐进式优化（不推倒重做、不动 DOM/数据/功能），把视觉完成度提到设计图 01 水平。全部改动为 CSS，集中在 fresh-natural.css 末尾追加「第三轮精修」段（同特异性后置覆盖前两轮），加 components.css Hero 微调。

**继续阅读/书架 = 首页视觉中心（§三/§五）**：
- 不等高错落：继续阅读 4 本 240/270/252/284px（书架页 6n 循环 240~280px），配 translateY(-4~9px) + rotate(±0.3~0.5°)——有设计感的不规则，非随机
- 信息区定高（min-height 76px）+ align-items:end：书立在架板上，高矮差落在顶部，底部对齐稳定
- 章节 meta 在 01 主题下退出卡面（display:none，数据仍在），卡面只剩 书名/作者/细线+百分比/心形
- hover = 书被轻轻拿起：封面转正浮起、影子变浅，无放大无发光

**实体书感（§三.2）**：圆角收到 2px/5px；切口侧 ::before 三道发丝页缘（repeating-linear-gradient）；书脊合页线保留（第二轮 ::after）；投影改「桌面投影」——0 16px 30px -12px 暖灰绿 26% + 2px 米色纸页硬边，杜绝黑色 Card 阴影。

**背景局部雾化（§七，不整体降 opacity）**：去掉 blur（细节保留但退后），叠加横向纸色渐变面纱（中央 40%~62% 区 0.64 alpha，两侧归零）——四角植物可见、中央内容区退后；contrast(0.86) saturate(0.82) brightness(1.05) 压低局部高亮。

**Sidebar 纸质目录（§八）**：62% 纸底 + 纸纹 + blur(8px)，无边框，影子降到 0 1px 8px 4%；选中态淡绿 75% 无阴影。

**色彩克制（§九）**：btn-primary 用 color-mix 混入 14% 灰绿降饱和、去光晕——不再像普通 Web App 的绿。

**首屏路径（§六）**：Hero min-height 250→218、标题上限 32px、间距再收——1440×900 首屏可见 Hero+继续阅读标题+封面主体，1920×1080 完整见书。

**进度条**：1.5px、rgba 柔绿、圆角 1px；百分比 10px 轻字；心形平时 35% 透明度、hover 显现（幽灵化，不是 UI Button）。

**保留清单核验**：主题切换/localStorage/搜索/Ctrl+K/侧栏/分类筛选/继续阅读/进度/收藏心形/今日书签/响应式/DOM 数据绑定全部未动；02~06 主题零影响（精修全部在 [data-theme="fresh-natural"] 内；Hero 尺寸为全局但方向与上轮一致）。

**验证**：由用户执行，重点 1440×900 / 1920×1080 的错落感、背景雾化浓度、首屏见书速度。

---

## 2026-09-03 · 侧栏不滚动 + 条目精简

用户反馈：侧栏出现内部滚动很难看；右侧内容滚动即可；按显示内容适度删减侧栏条目。

**条目精简（side-nav.js）**：主目录 7→5（首页/书架/笔记/书摘/设置）——「分类」并入书架页（点书架即达，书架页内有完整分类条），「时间线」无内容暂不展示；「我的分类」6→3（只留藏书最多的 文学/心灵成长/生活智慧，完整分类在书架页）。

**不滚动守卫（layout.css）**：.side-nav 加 max-height: calc(100vh - 104px) + overflow: hidden——桌面态任何窗口高度都不出内部滚动条；@media (max-height:820px) 隐藏纯装饰的名言+盆栽，功能项永不受影响；≤900 横滑条形态解除钳制（max-height:none / overflow:visible，横滑由 ul 内部承担）。

**高度核算**：精简后全量约 604px（5×46 主项 + 分隔/标签 + 3×46 分类 + 名言 + 盆栽），900 高视口可视区 ≈796px 富余；矮屏藏装饰后 ≈454px。

**功能核验**：syncNav 靠 data-view 匹配不受删项影响；分类筛选双入口保留（书架页芯片 + 侧栏我的分类前 3）。

---

## 2026-09-03 · 01 主题第四轮：消除最后的 Dashboard 残留 + 出版物气质

依据对设计图的批评清单（数据卡抢戏 / 书小且假 / 卡片泛滥 / 排版弱 / 情绪缺失）执行 5 项优化。CSS 集中在 fresh-natural.css 末尾「第四轮」段，01 作用域隔离；仅时辰问候为全局文案。

**① 今日书签去卡片化**：bookmark-card 从「圆角白卡+阴影」改为贴在纸上的便签——note-paper 底 + 纸纹 + 3/12px 不对称圆角 + 4/12px 轻影 + 微倾（-0.7° / 第二张 +0.9° 且下移 10px 制造错落）+ 左上胶带（与 Hero 手记同语言）；图标从圆角色块退化为一枚 60% 透明小书签；引文改 note-text 色 15px/2 行高；页脚加发丝虚线分隔。hover 转正浮起 2px。

**② 封面出版物气质**：`.cover__img` 加 `outline: 1px rgba(255,253,246,.42); outline-offset: -7px` = 距封面边 7px 的装饰细框；`::after` 叠加层重写，底部加 17% 高的极淡纸色渐隐带 = 真实封面留给书名的题字区；书脊合页线与纸纹保留。（未重生成图片，CSS 方案可逆；如需更强出版物感可后续 AI 重绘带题字区的封面）

**③ 统计彻底注脚化**：stats-grid 从 4 列网格改 flex 横排一行（gap 4px 0 防窄屏换行挤压），stat-card 去底线去内距改内联，图标 display:none，数字降到 19px 用 primary-soft-text 色、标签 11.5px/70%，项间发丝竖线分隔——形如「36 天 陪伴阅读 │ 8.6 小时 本周阅读 │ …」；区标题降 17px/85%。层级确定为 书区 22px > 分类 18px > 统计 17px。
　　踩坑修正：`.stat-card > div` 特异性 (0,2,1) 会反超图标的 `display:none` (0,2,0) 导致图标复现，改 `> div:last-child` 精确命中内容容器。

**④ Hero 文学排版**：标题左上加 52px 淡绿大引号「（16% 透明，absolute，窄屏隐藏）；徽章去胶囊底改「藏书印」——1px 淡绿细框 + 3px 小圆角 + 0.18em 大字距。

**⑤ 时辰问候（全局文案）**：hero.js 新增 greeting()，按当前小时给 7 档生活式提醒（清晨/上午/午后/下午/傍晚/夜里/深夜），渲染为 hero__greeting 衬线斜体 12.5px + 前置 18px 发丝短线。纯文案，不接数据层，无动画。

**核验**：功能/DOM 结构/数据/主题切换/02~06 样式零改动（第四轮全部在 [data-theme="fresh-natural"] 内；仅问候文案为全主题共用）。括号平衡检查通过。验证由用户执行。

---

## 2026-09-03 · 第五轮：壁纸固定化 + Hero 拿回主视觉（含绘制层级 bug 修复）

用户反馈：Hero 等于没有（全靠一张背景图），且背景随滚动变化不好看；Hero 应有独立主视觉图，壁纸则轻量化。

**★ 顺带修掉一个真实 bug（绘制层级）**：`.env-art` 是 positioned + z-index:0，而书卡/统计卡是 static 正常流——按 CSS 绘制顺序，z-index:0 的定位元素会绘制在 static 内容**之上**，即背景实际压在书卡上方。这正是前几轮反复出现的"背景压住内容/整体发糊"的根因，之前只靠降 opacity 治标。修法：`.app` 与 `.site-footer` 加 `position: relative; z-index: 1`，把内容区明确抬到壁纸之上。

**壁纸轻量化 + 固定（layout.css）**：`.env-art` 从 `absolute; height:min(88vh,920px)`（随文档滚动）改为 `fixed; inset:0; height:100%`——滚动时壁纸纹丝不动，真正像壁纸；opacity 全局 0.42→0.26；mask 从"底部全透明"改为平缓衰减（底部保留 38%），避免滚到页面下半部背景突然变纯纸。background-position 改 `center top, right top`（glow 居中、壁纸重心右上）。

**Hero 主视觉（components.css）**：`.hero` 改 grid 左文右图（`minmax(0,1fr) minmax(0,44%)`，gap 30，min-height 300）；`.hero__scene` 桌面端从 `display:none` 恢复为 block，高度 `clamp(230px,25vw,330px)`，圆角 6/20px（非卡片式均匀圆角），投影用 `color-mix(--text)` 双层柔和影；`::before` 从横向 `--hero-fade` 改为顶部微光 + 底部微影（全部用主题令牌 color-mix，暗色主题同样成立）——图成为"挂在书房墙上的一幅画"，与极淡壁纸形成两级层次。

**手记位置**：从 Hero 右上（top:16 right:32）移到场景图右下角（bottom:18 right:20），不遮画的主体、不溢出 Hero，空间逻辑=便签贴在画角。

**01 主题**：壁纸 opacity 降到 0.18（比全局更轻）+ 保留横向纸色面纱 + 补 `background-position` 三值（面纱/glow/壁纸三层，不能继承全局两值简写）；Hero 画框加纸色发丝描边 + 暖灰绿柔投影。清理了第三轮遗留的重复 `.env-art` 规则（否则 opacity 0.3 会覆盖新值）。

**dream 主题**：其全屏壁纸就是 hero.png 本身，桌面端（>900）隐藏 `.hero__scene` 并把 hero 收成单列，避免"图上图"；≤900 纵排保持原样。

**移动端（≤900）**：hero 卡片壳内的场景图去圆角/去描边/去投影（壳自带圆角+overflow，避免双重边界）；壁纸仍 display:none。

**未动**：DOM 结构、数据、路由、功能、02~06 主题令牌（仅 dream 加两条防重复规则）。括号平衡与规则唯一性检查通过。验证由用户执行。

---

## 2026-09-03 · 第六轮：Hero 推倒「画框」改通铺场景（修上一轮方向性错误）

用户看图指出 Hero 很丑：图只占右半边、位置突兀、有明显卡片感。承认上一轮方向错误——把 Hero 图做成「挂在墙上的画」（圆角+描边+投影+右侧 44% 分栏），本质就是一张卡片，而且左文右图两块之间构图是断的，手记卡叠在图上又形成「卡片套卡片」。

**Hero 改通铺场景（components.css）**：
- `.hero` 去掉 grid 分栏，回到 block + relative，min-height `clamp(300px,30vw,400px)`；无框、无圆角、无阴影
- `.hero__scene` 改 `position:absolute; inset:0` 铺满整个 Hero——它不是一幅画，而是书房本身
- `img` object-fit cover + `object-position: center 62%`（01 主题微调到 66%，让桌面与窗光落在中部）
- `::before` **三缘羽化**（左/顶/底）：左缘纸色从 100% 渐隐到 76% 处透明（同时保证文字可读），顶缘与底缘各羽化 30%/34%——消除全部矩形边界，图从纸面里「长出来」，与顶栏及下方继续阅读自然衔接。全部用 `color-mix(in srgb, var(--bg) …)` 主题令牌，暗色主题同样成立
- `.hero__text` max-width 620→540、padding 加到 44px、`min-height: inherit` 垂直居中填满 Hero

**手记卡弱化（components.css）**：从「实心白底+重投影浮在图上」改为「蒙在场景上的半透明纸」——`color-mix(--note-paper 84%, transparent)` + `backdrop-blur(6px)`，投影改 `color-mix(--text)` 令牌化并减轻，宽 300→272、圆角 4/14→3/12、胶带缩到 48×14 且 opacity 0.7。位置 bottom 26 / right 30。

**01 主题**：删掉上一轮加的 `.hero__scene` 画框描边+投影（卡片感的直接来源），只保留 object-position 微调；today-note 投影略调（半透明纸需要一点分层影，0 5px 14px -6px / 0.16）。

**dream 主题**：删掉失效的 `.hero { grid-template-columns }`（hero 已不是 grid），保留桌面端隐藏 hero__scene（其全屏壁纸即 hero.png，避免图上图）。

**移动端 ≤900**：`.hero__scene` 补 `inset:auto` 明确取消 absolute 铺满，改回 relative + 固定高参与纵排；`.hero__text` 补 `min-height:0`；`::before` 的硬编码 `rgba(247,237,216,…)` 令牌化为 `color-mix(var(--bg))`（顺手清一处设计债）。

**层级关系（最终）**：壁纸 fixed / 0.26（01: 0.18）极淡不动 → Hero 通铺场景较实、三缘溶进纸面 → 文字与书卡 z-index 抬升最清晰。三级递进，无一处矩形容器。

**验证**：由用户执行。可调项：Hero 高度上限 400、图取景 object-position 62%/66%、左缘渐隐终点 76%（想让图更实就降到 62%）、手记透明度 84%。
